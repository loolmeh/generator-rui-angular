'use strict';

/**
 * @ngdoc function
 * @name <%= module %>.controller:<%= name %>_ctrl
 * @description
 * # <%= name %>_ctrl
 * Controller of the <%= module %>
*/

angular.module('<%= module %>')
    .controller('<%= name %>_ctrl', function ($scope) {
        // do stuff on the $scope
        $scope.sth = 42;

    });
