'use strict';

/**
 * @ngdoc service
 * @name <%= module %>.<%= name %>
 * @description
 * # <%= name %>
 * Constant in the <%= module %> module.
 */

angular.module('<%= module %>')
    .constant('<%= name %>', 42);
