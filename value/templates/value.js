'use strict';

/**
 * @ngdoc service
 * @name <%= module %>.<%= name %>
 * @description
 * # <%= name %>
 * Value in the <%= module %> module.
 */

angular.module('<%= module %>')
    .value('<%= name %>', 42);
